package com.morgan.grid.client.common;

/**
 * A common interface implemented by the main app presenter for an application.
 *
 * @author mark@mark-morgan.net (Mark Morgan)
 */
public interface AppPresenter {
  /**
   * The callback method invoked by a GWT application's onModuleLoad method.
   */
  void startApp();
}
